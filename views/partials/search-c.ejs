<!-- public/partials/seach-c.ejs -->
<div class="container">
    <div class="card card-container">
        <h2>Search for "
            <%= searchterm %>"</h2>
        <%if (users.length==0) { %>
            <% include ../partials/searcherr-c %>
        <% } else { %>
            <% include ../partials/searchresults-c %>
        <% } %>
    </div>
</div>